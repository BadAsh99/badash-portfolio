# Session Notes — 2026-03-28

## What Was Built

### Visual Redesign
- Color scheme: electric blue (#0080ff) primary + red (#ff2222) accent on deep navy (#04040e)
- Dark glassmorphism: `glass-card` CSS class with backdrop-filter blur, floating radial orbs (inline styles)
- Float animations added to Tailwind config: `animate-float-slow/medium/fast`

### Hero Section
- 3-column layout: LLMGuardT2 terminal boot | HUD profile image (center) | CloudGuard terminal boot
- `TerminalBoot` component: typewriter animation with configurable `BootLine[]` arrays
- `LLMGUARDT2_LINES` and `CLOUDGUARD_LINES` exported from `TerminalBoot.tsx`
- CTA buttons + stat grid appear after both boot sequences complete
- Grid overlay has `pointer-events-none` (was blocking nav clicks)

### HUD Profile Image
- File: `apps/web/public/ash-profile.jpeg` (Midjourney binary-code hacker figure)
- Component: `components/shared/HUDProfile.tsx`
- Features: corner targeting brackets (SVG), animated scan line, tick marks, data strips, pulsing border glow
- Keyframes in `globals.css`: `scanLine`, `hudPulse`
- Used in hero (lg: center column, mobile: below terminals) and about page (sm size, right side)

### About Page (`app/about/page.tsx`)
- Split header: identity text left + HUD image right
- Rock-star bio: hardened gas & oil / telecom / Fortune 500 networks
- Stats bar: Fortune 500, Gas & Oil, Telecom, PCNSE
- Career timeline: Palo Alto Networks (ACTIVE) → American Express → Allied Telecom Group
- Capabilities grid (glass-card): AI Security, SASE & PAN-OS, Cloud Security, Security Engineering
- PCNSE badge, Phoenix AZ, ECPI University tags

### Security Threat Feed
- API route: `app/api/security-news/route.ts`
  - Fetches CISA KEV (top 8 recently added exploited CVEs)
  - Fetches NVD CVE API v2 (top 6 HIGH severity)
  - Combines, sorts by date, returns top 10 with `revalidate: 3600`
- Component: `components/security/SecurityFeed.tsx`
  - Severity color coding: CRITICAL=red, HIGH=orange, MEDIUM=yellow
  - Refresh button, last-updated timestamp, skeleton loading, fallback empty state
  - Placed on homepage between Projects and GitHub sections

### Projects Bento Grid (`components/projects/ProjectsSection.tsx`)
- `badash-killchain` — FeaturedCard, `lg:col-span-2` (hero/wide card with all highlights)
- `llmguardt2`, `cloudguard` — FeaturedCard (1 col, with highlights)
- `firewise-ai`, `llmguard`, `posture-validator` — CompactCard (description + tags only)
- All cards link to GitHub, show status badge (ACTIVE/STABLE/WIP) and live GitHub stats

## Bugs Fixed
| Bug | Fix |
|-----|-----|
| Railway builds failing | TypeScript error in `security-news/route.ts` — `Record<string,unknown>` can't be indexed by `[0]`; typed as `Record<string, Array<...>>` |
| Nav links blocked | Grid overlay in HeroSection missing `pointer-events-none` |
| Terminal text clipped | `overflow-hidden` moved from `.terminal-chrome` wrapper to `.terminal-chrome-header` only |
| Tailwind orb classes purged | All orb/gradient styling converted to inline React `style={{}}` |
| Red orb off-screen | Was at `bottom: -10rem`; moved to `bottom: -2rem` with opacity boosted |
| `railway up` from wrong dir | Must run from `apps/web/`, not monorepo root |

## Deployment
- GitHub repo: `BadAsh99/badash-portfolio`
- Railway: `badash-portfolio` service (web), `delightful-courage` service (llmguardt2)
- Domains: `badash99.dev` + `ashclements.dev` → both point to web service
- Deploy via `git push origin main` (GitHub-connected auto-deploy)
- Always run `npm run build` in `apps/web/` before pushing

## Pending
- [ ] ashclements.dev differentiation (more LinkedIn/resume feel vs badash99 hacker)
- [ ] Blog posts: week-03+ (BadAshWednesdays content)
- [ ] LinkedIn PDF import for more career detail on About page
- [ ] Verify terminal windows render correctly once latest deploy lands
